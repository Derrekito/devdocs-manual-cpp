# std::hash<std::error_code>

```cpp
template<> struct hash<std::error_code>;  // (since C++11)
```

The template specialization of `std::hash` for `std::error_code` allows users to
obtain hashes of objects of type `std::error_code`.

---
*Source: https://en.cppreference.com/w/cpp/error/error_code/hash*
