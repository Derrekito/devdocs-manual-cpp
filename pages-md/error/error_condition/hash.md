# std::hash<std::error_condition>

```cpp
template<> struct hash<std::error_condition>;  // (since C++17)
```

The template specialization of `std::hash` for `std::error_condition` allows
users to obtain hashes of objects of type `std::error_condition`.

---
*Source: https://en.cppreference.com/w/cpp/error/error_condition/hash*
