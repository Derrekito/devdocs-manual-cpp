# C++ keyword: co_await (since C++20)

The unary operator `co_await` suspends a `coroutine` and returns control to the
caller.

### Usage

- `co_await` expression (since C++20)

---
*Source: https://en.cppreference.com/w/cpp/keyword/co_await*
