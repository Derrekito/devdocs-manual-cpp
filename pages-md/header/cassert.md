# Standard library header <cassert>

This header was originally in the C standard library as `<assert.h>`.

This header is part of the error handling library.

**Macros**

- **assert** — aborts the program if the user-specified condition is not `true`.
  May be disabled for release builds (function macro)

### Synopsis

```cpp
#define assert(E) /* see description */
```

---
*Source: https://en.cppreference.com/w/cpp/header/cassert*
